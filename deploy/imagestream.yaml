# ImageStream for the Review (review-agent) container image.
# After build, reference e.g.
# image-registry.openshift-image-registry.svc:5000/<namespace>/review-agent:<tag>,
# matching your cluster's internal registry and tag policy.
apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: review-agent
  labels:
    app: review-agent
spec: {}
